[pytest]
# `pythonpath = .` torna o pacote `app` importável a partir da raiz apps/analytics.
pythonpath = .
testpaths = tests
markers =
    integration: testes que precisam de um Postgres+PostGIS real (container efémero)
